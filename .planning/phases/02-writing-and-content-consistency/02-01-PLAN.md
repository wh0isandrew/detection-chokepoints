---
phase: 02-writing-and-content-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chokepoints/initial-access/clickfix-techniques.yml
  - chokepoints/initial-access/renamed-rmm-tools.yml
  - chokepoints/defense-evasion/ransomware-service-manipulation.yml
  - chokepoints/defense-evasion/byosi-scripting-interpreters.yml
  - chokepoints/defense-evasion/edr-bypass-techniques.yml
  - chokepoints/persistence/web-shells.yml
  - chokepoints/credential-access/browser-credential-theft.yml
  - chokepoints/lateral-movement/remote-execution-tools.yml
  - trends/clickgrab.md
  - trends/index.md
  - trends/masq-infra.md
autonomous: true
requirements: [WRIT-01, WRIT-02, WRIT-03]
must_haves:
  truths:
    - "No prose em dash (U+2014) appears in any chokepoint YAML Description, Notes, WhyCantBypass, or References.Name field"
    - "No prose em dash appears in any trends/*.md authored prose paragraph"
    - "Description fields read in Tyler's voice: short sentences, evidence-first, no throat-clearing"
    - "Table placeholder dashes in Liquid `default: \"—\"` filters are preserved (data notation, not prose)"
  artifacts:
    - path: "chokepoints/credential-access/browser-credential-theft.yml"
      provides: "Voice-edited Description, em dashes removed"
    - path: "chokepoints/persistence/web-shells.yml"
      provides: "Voice-edited Description"
    - path: "chokepoints/initial-access/clickfix-techniques.yml"
      provides: "Em dashes removed from Variation Notes, weak closer cut"
    - path: "trends/clickgrab.md"
      provides: "Em dashes removed from prose"
  key_links:
    - from: "chokepoints/*.yml"
      to: "_data/chokepoints.yml"
      via: "scripts/aggregate.py"
      pattern: "yaml.safe_load"
---

<objective>
Strip every prose em dash from public-facing authored content (8 chokepoint YAMLs and 3 trends Markdown files) and tighten Description-field voice to match Tyler's register (peer-to-peer, evidence-first, no filler). Preserve table placeholder dashes in Liquid `default` filters and date-range en dashes — those are data notation, not prose.

Purpose: Phase 2 success requires no em dashes in rendered prose and consistent voice. This plan owns content edits in source files; Wave 2 owns template edits.
Output: 8 YAML files and 3 Markdown files with em dashes removed from prose and Description-field voice tightened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-writing-and-content-consistency/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Em dash removal across all YAML and Markdown prose</name>
  <files>
    chokepoints/initial-access/clickfix-techniques.yml,
    chokepoints/defense-evasion/edr-bypass-techniques.yml,
    chokepoints/lateral-movement/remote-execution-tools.yml,
    trends/clickgrab.md,
    trends/index.md,
    trends/masq-infra.md
  </files>
  <read_first>
    - .planning/phases/02-writing-and-content-consistency/02-RESEARCH.md (Em Dash Audit section, lines 32-110)
    - chokepoints/initial-access/clickfix-techniques.yml (lines 100-150)
    - chokepoints/defense-evasion/edr-bypass-techniques.yml (line 131 area)
    - chokepoints/lateral-movement/remote-execution-tools.yml (lines 275-315)
    - chokepoints/defense-evasion/ransomware-service-manipulation.yml (lines 135-145 — verify Windows Event Log strings stay intact per Pitfall 4)
    - trends/clickgrab.md, trends/index.md, trends/masq-infra.md
  </read_first>
  <action>
    Remove every prose em dash (U+2014, `—`) from the listed files. DO NOT use a global find-replace — table placeholder dashes in `| default: "—"` Liquid filters and Windows Event Log description quotes must survive.

    **YAML edits — replace exactly as shown:**

    1. `chokepoints/initial-access/clickfix-techniques.yml` line ~103 (Variation Note):
       BEFORE: `troubleshot.sh (note typo in filename — "troubleshot" not "troubleshoot")`
       AFTER:  `troubleshot.sh (note typo in filename: "troubleshot" not "troubleshoot")`

    2. `chokepoints/initial-access/clickfix-techniques.yml` line ~145 (Variation Notes):
       BEFORE: `create urgency — the victim believes`
       AFTER:  `create urgency. The victim believes`

    3. `chokepoints/initial-access/clickfix-techniques.yml` Description weak closer (search for `not just commodity crimeware`):
       BEFORE: `APT28, MuddyWater, and Kimsuky adopted ClickFix for espionage in late 2024. This is not just commodity crimeware anymore.`
       AFTER:  `APT28, MuddyWater, and Kimsuky adopted ClickFix for espionage in late 2024.`
       (Cut the second sentence entirely. The actor list makes the point.)

    4. `chokepoints/defense-evasion/edr-bypass-techniques.yml` line ~131 (Context field):
       BEFORE: `No process termination events —`
       AFTER:  `No process termination events.` (rewrite the surrounding sentence so the period works; if the original is `No process termination events — only loaded module events`, change to `No process termination events. Only loaded module events.`)

    5. `chokepoints/lateral-movement/remote-execution-tools.yml` References Name fields (lines ~280, 285, 289, 294, 310). For each entry of the form `Name: "MITRE ATT&CK — T1021.003"`:
       BEFORE: `Name: "MITRE ATT&CK — T1021.003"`
       AFTER:  `Name: "MITRE ATT&CK: T1021.003"`
       Apply the same colon substitution to every `References[].Name` value containing ` — ` in this file.

    6. `chokepoints/defense-evasion/ransomware-service-manipulation.yml` lines ~139-140 (LogSources): LEAVE UNCHANGED. The em dashes inside `Service State Change — stopped` and `Service Start Type Changed — disabled` quote official Windows Event Log channel description text (Pitfall 4 in RESEARCH.md).

    **Markdown edits — replace exactly as shown:**

    7. `trends/clickgrab.md` front matter title:
       BEFORE: `title: "ClickFix Delivery Chain — Trend Analysis"`
       AFTER:  `title: "ClickFix Delivery Chain: Trend Analysis"`
       Apply the same fix to the `<h1>` near line 158.

    8. `trends/clickgrab.md` line ~196:
       BEFORE: `Every ClickFix variant — ClickFix, FileFix, TerminalFix, InstallFix — follows these five stages.`
       AFTER:  `Every ClickFix variant (ClickFix, FileFix, TerminalFix, InstallFix) follows these five stages.`

    9. `trends/clickgrab.md` line ~240:
       BEFORE: `— the Nov 2025 surge was driven by`
       AFTER:  Restructure surrounding sentence. End the previous clause with a period; begin new sentence: `The Nov 2025 surge was driven by...`

    10. `trends/clickgrab.md` line ~249:
        BEFORE: `defenders tuned IWR/IEX-specific detections — and why that rotation actually validates the chokepoint approach.`
        AFTER:  `defenders tuned IWR/IEX-specific detections. That rotation validates the chokepoint approach.`

    11. `trends/clickgrab.md` line ~257 (two em dashes in callout):
        BEFORE: `Dec 2025 pivot — your IWR detections worked. ... That's not coincidence — that's adversaries responding to detection pressure.`
        AFTER:  `Dec 2025 pivot. Your IWR detections worked. ... That's not coincidence. That's adversaries responding to detection pressure.`

    12. `trends/index.md` line ~184:
        BEFORE: `Chokepoints stay stable — but the techniques`
        AFTER:  `Chokepoints stay stable. The techniques...` (split into two sentences)
        Apply analogous "split at em dash, two sentences" treatment to lines ~201, ~211, ~385, ~413. Read each line in context and choose period or comma based on which gives the cleaner two-clause result.

    13. `trends/masq-infra.md`:
        - Front matter `description` line ~4: BEFORE `— the domains that served malicious binaries` AFTER: rewrite as a complete sentence joined with a colon or as a new clause without the em dash.
        - Line ~257: BEFORE `— chain coverage is` AFTER: split into new sentence beginning with `Chain coverage is...`
        - Line ~417: BEFORE `— shared favicon hash, shared IP address` AFTER: replace ` — ` with `: ` so it reads as a list intro.
        - LEAVE UNCHANGED: lines 486, 487, 488, 559, 561, 565, 570 — these are `| default: "—"` Liquid filters producing data-table empty-cell markers. They are NOT prose punctuation.

    After all edits, validate YAML parses:
    `python -c "import yaml; [yaml.safe_load(open(f)) for f in ['chokepoints/initial-access/clickfix-techniques.yml','chokepoints/defense-evasion/edr-bypass-techniques.yml','chokepoints/lateral-movement/remote-execution-tools.yml']]; print('OK')"`
  </action>
  <verify>
    <automated>python -c "import yaml; [yaml.safe_load(open(f)) for f in ['chokepoints/initial-access/clickfix-techniques.yml','chokepoints/defense-evasion/edr-bypass-techniques.yml','chokepoints/lateral-movement/remote-execution-tools.yml','chokepoints/defense-evasion/ransomware-service-manipulation.yml']]; print('YAML OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n $'\u2014' chokepoints/initial-access/clickfix-techniques.yml` returns zero matches
    - `grep -n $'\u2014' chokepoints/defense-evasion/edr-bypass-techniques.yml` returns zero matches
    - `grep -n $'\u2014' chokepoints/lateral-movement/remote-execution-tools.yml` returns zero matches
    - `grep -c "Service State Change" chokepoints/defense-evasion/ransomware-service-manipulation.yml` is >= 1 (Windows Event Log line preserved)
    - `grep -n $'\u2014' trends/clickgrab.md` returns zero matches
    - `grep -n $'\u2014' trends/index.md` returns zero matches
    - `grep -n $'\u2014' trends/masq-infra.md` returns ONLY lines containing `| default:` (Liquid table placeholders preserved)
    - `grep -c 'default: "—"' trends/masq-infra.md` returns >= 5 (table placeholders intact)
    - `grep -F 'not just commodity crimeware' chokepoints/initial-access/clickfix-techniques.yml` returns zero matches (weak closer cut)
    - `grep -F 'ClickFix Delivery Chain: Trend Analysis' trends/clickgrab.md` returns >= 2 matches (front matter + h1)
    - YAML files all parse successfully via the python yaml.safe_load command above
  </acceptance_criteria>
  <done>Every prose em dash in the targeted files removed; Windows Event Log quoted strings and Liquid table placeholders preserved; all YAML still parses.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Voice tightening pass on Description fields (5 chokepoint YAMLs)</name>
  <files>
    chokepoints/credential-access/browser-credential-theft.yml,
    chokepoints/persistence/web-shells.yml,
    chokepoints/initial-access/renamed-rmm-tools.yml,
    chokepoints/defense-evasion/byosi-scripting-interpreters.yml,
    chokepoints/defense-evasion/ransomware-service-manipulation.yml
  </files>
  <read_first>
    - .planning/phases/02-writing-and-content-consistency/02-RESEARCH.md (Voice Audit section, lines 217-271)
    - CLAUDE.md (Author Context + AI Assistance Guidelines)
    - chokepoints/credential-access/browser-credential-theft.yml (full Description block)
    - chokepoints/persistence/web-shells.yml (full Description block)
    - chokepoints/initial-access/renamed-rmm-tools.yml (Description block)
    - chokepoints/defense-evasion/byosi-scripting-interpreters.yml (Description block)
    - chokepoints/defense-evasion/ransomware-service-manipulation.yml (Description block)
    - chokepoints/initial-access/clickfix-techniques.yml lines 11-18 (Description — REFERENCE for target voice; do not edit here, edited in Task 1)
  </read_first>
  <action>
    Rewrite Description fields in 5 YAMLs to match the ClickFix register. Do not change any other field. Do not change YAML structure or quoting style. Preserve all stat numbers, source citations, and actor names exactly.

    **Target voice (from clickfix-techniques.yml Description, the benchmark):**
    > "User pastes a malicious command from their clipboard into a Run dialog, terminal, or Explorer address bar. The lure page writes to the clipboard via JavaScript and the user does the rest. No attachment, no exploit, no macro. The clipboard is the delivery mechanism and the scripting interpreter spawn is the chokepoint."

    Rules:
    - Open with the invariant as a statement of fact, not an announcement of the invariant.
    - Cut "systematically", "in order to", "Adversaries plant" passive openings, "This is the single...", "It should be noted", and similar throat-clearing.
    - Sentences should be short. Stat dumps get their own sentence.
    - No em dashes in any rewrite.
    - Preserve every numeric stat and every source attribution.

    **Edit 1 — `browser-credential-theft.yml` Description (lines ~17-25):**

    BEFORE:
    ```
    Infostealers systematically harvest credentials, cookies, and autofill data from browser credential databases. This is the single invariant behavior across all stealer families regardless of obfuscation or bypass technique. Hudson Rock tracks 30+ million infected computers; 1.8 billion credentials were stolen in 2025 alone, with enterprise credentials present in 14% of infections (up from 6% in early 2024).
    ```

    AFTER:
    ```
    Every infostealer reads the same files: the browser's credential database, cookie store, and autofill cache. Family, packer, and bypass technique change. The target paths do not. Hudson Rock tracks 30+ million infected computers. 1.8 billion credentials were stolen in 2025. Enterprise credentials appeared in 14% of infections, up from 6% in early 2024.
    ```

    **Edit 2 — `web-shells.yml` Description (lines ~17-25):**

    BEFORE:
    ```
    Adversaries plant web-accessible scripts (web shells) on compromised servers to maintain persistent command execution via HTTP/HTTPS. Web shells are deployed in virtually every major web-facing compromise, appearing in 35% of Q4 2024 IR incidents (Cisco Talos) and serving as the primary persistence mechanism in ProxyLogon, ProxyShell, MOVEit, Barracuda ESG, and Ivanti zero-day campaigns.
    ```

    AFTER:
    ```
    A script lives in the web root and the server executes it on request. That is the entire mechanism. Web shells appeared in 35% of Q4 2024 IR incidents (Cisco Talos) and were the primary persistence layer for ProxyLogon, ProxyShell, MOVEit, Barracuda ESG, and the Ivanti zero-day campaigns. Polyglots, fileless IIS modules, and steganography do not change the chokepoint: a request hits a server-side interpreter that should not be executing attacker-controlled code.
    ```

    **Edits 3-5 — `renamed-rmm-tools.yml`, `byosi-scripting-interpreters.yml`, `ransomware-service-manipulation.yml`:**

    For each: read the current Description field, identify and remove throat-clearing openers ("Adversaries leverage", "Threat actors utilize", "It is common for", etc.), shorten sentences, lead with the invariant. Preserve every stat and citation. Match the ClickFix register. Do not introduce em dashes.

    Each rewrite must:
    - Be no longer than the original (measured in characters)
    - Open with a concrete behavior or fact, not "Adversaries..." or "This technique..."
    - Contain at least one ≤8-word sentence
    - Contain zero em dashes (`—`)

    After each edit, validate YAML parses:
    `python -c "import yaml; yaml.safe_load(open('chokepoints/...')); print('OK')"`
  </action>
  <verify>
    <automated>python -c "import yaml; [yaml.safe_load(open(f)) for f in ['chokepoints/credential-access/browser-credential-theft.yml','chokepoints/persistence/web-shells.yml','chokepoints/initial-access/renamed-rmm-tools.yml','chokepoints/defense-evasion/byosi-scripting-interpreters.yml','chokepoints/defense-evasion/ransomware-service-manipulation.yml']]; print('YAML OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 YAML files parse via the python command above
    - `grep -F 'systematically harvest' chokepoints/credential-access/browser-credential-theft.yml` returns zero matches
    - `grep -F 'This is the single invariant' chokepoints/credential-access/browser-credential-theft.yml` returns zero matches
    - `grep -F 'Hudson Rock' chokepoints/credential-access/browser-credential-theft.yml` returns >= 1 (stat preserved)
    - `grep -F '1.8 billion credentials' chokepoints/credential-access/browser-credential-theft.yml` returns >= 1
    - `grep -F 'Adversaries plant web-accessible' chokepoints/persistence/web-shells.yml` returns zero matches
    - `grep -F 'ProxyLogon' chokepoints/persistence/web-shells.yml` returns >= 1 (campaign list preserved)
    - `grep -F '35% of Q4 2024' chokepoints/persistence/web-shells.yml` returns >= 1
    - `grep -cF $'\u2014' chokepoints/credential-access/browser-credential-theft.yml chokepoints/persistence/web-shells.yml chokepoints/initial-access/renamed-rmm-tools.yml chokepoints/defense-evasion/byosi-scripting-interpreters.yml` shows 0 in the Description blocks (verify the file as a whole if needed; 0 total prose em dashes for these files)
    - For each of the 5 files, the new Description field has the same or fewer characters than the original (compare with `git diff --stat`)
  </acceptance_criteria>
  <done>5 Description fields rewritten in Tyler's voice; all stats and citations preserved; all 5 YAML files parse; no em dashes introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML source → aggregate.py → _data/chokepoints.yml → Liquid render → static HTML | Single-author content pipeline; no untrusted input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | YAML edit introduces syntax error breaking aggregate.py | mitigate | Run `python -c "import yaml; yaml.safe_load(open(f))"` after each YAML edit (Pitfall 3) |
| T-02-02 | Information Disclosure | None — all content is public-facing detection knowledge | accept | No PII, no secrets in scope |
| T-02-03 | Tampering | Removing em dash from Windows Event Log quoted text changes factual claim | mitigate | Pitfall 4 — preserve LogSources strings verbatim; only edit free-form prose |
</threat_model>

<verification>
- All YAML files parse via `python -c "import yaml; yaml.safe_load(open(f))"`
- `grep -rn $'\u2014' chokepoints/` returns ONLY the preserved Windows Event Log lines in `ransomware-service-manipulation.yml`
- `grep -rn $'\u2014' trends/*.md` returns ONLY lines containing `default:` (Liquid table placeholders)
- All preserved-content acceptance grep checks pass (Hudson Rock, ProxyLogon, 35% Q4 2024, etc.)
</verification>

<success_criteria>
- WRIT-01 satisfied for all 8 chokepoint YAMLs and 3 trends Markdown files (template em dashes handled in Wave 2 plan 02)
- WRIT-02 satisfied for 5 rewritten Description fields (clickfix already at target register)
- WRIT-03 satisfied: Description fields across all 8 chokepoint YAMLs read at the same register
- Table placeholder dashes and Windows Event Log quoted text preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-writing-and-content-consistency/02-01-SUMMARY.md`
</output>
