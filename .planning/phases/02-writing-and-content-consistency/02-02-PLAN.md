---
phase: 02-writing-and-content-consistency
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - _layouts/chokepoint.html
  - _layouts/default.html
autonomous: true
requirements: [WRIT-01, WRIT-04]
must_haves:
  truths:
    - "A first-time reader landing on any chokepoint detail page sees TheConstant in the page header without scrolling past tool lists or variant tables"
    - "No `&mdash;` HTML entity or raw `—` character appears in any rendered Liquid layout file outside Liquid comments"
    - "All 8 chokepoint detail pages render TheConstant from the YAML top-level field via the existing .cp-invariant CSS class"
    - "jekyll build --strict exits zero with no warnings or errors after the template edits"
  artifacts:
    - path: "_layouts/chokepoint.html"
      provides: "TheConstant rendered in detail-page header; all &mdash; entities replaced; inline prose em dash removed"
      contains: "cp.TheConstant"
    - path: "_layouts/default.html"
      provides: "Footer tagline em dash removed"
  key_links:
    - from: "_layouts/chokepoint.html header block"
      to: "cp.TheConstant (top-level YAML field)"
      via: "Liquid `{% if cp.TheConstant %}` guard rendering .cp-invariant block"
      pattern: "cp\\.TheConstant"
---

<objective>
Add `TheConstant` to the chokepoint detail-page header so the invariant is the visual anchor of every chokepoint page (WRIT-04). Strip every `&mdash;` entity and raw em dash from `_layouts/chokepoint.html` and `_layouts/default.html` (WRIT-01 template scope). Use only the existing `.cp-invariant` / `.cp-invariant-label` CSS classes — no new CSS, no color changes, no layout redesign.

Purpose: Wave 1 confirmed all 8 YAML files have `TheConstant` populated at the top level. The template currently never renders it on detail pages. Fixing this is the highest-impact change in Phase 2.
Output: Two layout files edited; jekyll build --strict passes clean; manual page load shows TheConstant above the fold on every chokepoint page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-writing-and-content-consistency/02-RESEARCH.md
@_layouts/chokepoint.html
@_layouts/default.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TheConstant invariant block to chokepoint detail-page header</name>
  <files>_layouts/chokepoint.html</files>
  <read_first>
    - _layouts/chokepoint.html (full file, but pay close attention to the `<header>` block around line 870-920 and the existing `.cp-invariant` / `.cp-invariant-label` style references near lines 148-166)
    - _includes/chokepoint-card.html (reference: this is where TheConstant currently renders truncated; mirror its access pattern `cp.TheConstant`)
    - chokepoints/initial-access/clickfix-techniques.yml (verify field name is exactly `TheConstant` at top level)
    - .planning/phases/02-writing-and-content-consistency/02-RESEARCH.md (WhyCantBypass / TheConstant Current Display section)
  </read_first>
  <action>
    Insert a `TheConstant` invariant block in the `<header>` of the chokepoint detail page, immediately after the `cp.Description` paragraph and before the byline (author / updated date / source link). Use the existing `.cp-invariant` and `.cp-invariant-label` CSS classes. Do not introduce new CSS, do not change colors, do not modify any other section of the layout.

    **Step 1 — locate the insertion point.** Find the `<header>` block in `_layouts/chokepoint.html` (approximately line 874). Within it, find the closing of the description paragraph (`{{ cp.Description | markdownify }}` or equivalent — read the file to confirm exact Liquid). The insertion point is the line immediately after the description paragraph closes and before the byline `<div>` opens.

    **Step 2 — insert this exact Liquid block:**

    ```liquid
    {% if cp.TheConstant %}
    <aside class="cp-invariant" aria-label="The invariant">
      <div class="cp-invariant-label">THE CONSTANT</div>
      <p>{{ cp.TheConstant }}</p>
    </aside>
    {% endif %}
    ```

    The `{% if cp.TheConstant %}` guard means YAML files without the field render nothing — but Wave 1 verification confirmed all 8 files have it.

    **Step 3 — fix the inline prose em dash at line ~944.** Find:
    BEFORE: `invariant condition the attacker must satisfy — regardless of tool`
    AFTER:  `invariant condition the attacker must satisfy, regardless of tool`

    **Step 4 — replace every `&mdash;` entity in sigma-label and det-meta-label UI chrome.** Find each occurrence and replace `&mdash;` with a colon `:`. Specific lines per RESEARCH.md (line numbers approximate; verify by reading file):

    - Line ~1203: `<span class="sigma-label">IOK Rule &mdash; {{ ed.Layer }}</span>` → `<span class="sigma-label">IOK Rule: {{ ed.Layer }}</span>`
    - Line ~1205: `<span class="sigma-label">Sigma Rule &mdash; {{ ed.Layer }}</span>` → `<span class="sigma-label">Sigma Rule: {{ ed.Layer }}</span>`
    - Line ~1310: `<span class="sigma-label">Sigma Rule &mdash; {{ det.Level }} Level</span>` → `<span class="sigma-label">Sigma Rule: {{ det.Level }} Level</span>`
    - Line ~1364: `<span class="sigma-label">Sigma Rule &mdash; Hunt Level (Network)</span>` → `<span class="sigma-label">Sigma Rule: Hunt Level (Network)</span>`
    - Line ~1400: `<div class="det-meta-label">Goal &mdash; {{ ed.Layer }}</div>` → `<div class="det-meta-label">Goal: {{ ed.Layer }}</div>` (note: research says raw `—` here; use grep to find both forms)
    - Line ~1413: `<span class="sigma-label">IOK Rule &mdash; {{ ed.Layer }}</span>` → `<span class="sigma-label">IOK Rule: {{ ed.Layer }}</span>`
    - Line ~1415: `<span class="sigma-label">Sigma Rule &mdash; {{ ed.Layer }}</span>` → `<span class="sigma-label">Sigma Rule: {{ ed.Layer }}</span>`
    - Line ~1469: `<span class="sigma-label">Sigma Rule &mdash; {{ level }} Level</span>` → `<span class="sigma-label">Sigma Rule: {{ level }} Level</span>`

    Use grep to verify all `&mdash;` instances were caught: `grep -n 'mdash' _layouts/chokepoint.html` should return zero matches after the edits (Liquid comments are fine to leave; if a comment at line ~1236 contains `—`, leave it — comments do not render to HTML).

    **Step 5 — DO NOT touch:**
    - The Liquid comment at line ~1236 (`{%- comment -%} ... — no sigma rules here {%- endcomment -%}`) — comments do not render
    - The detection-stage accordion structure
    - Any CSS or style tag
    - Any `forloop` logic
    - The `WhyCantBypass` rendering inside stage accordions (it stays where it is — TheConstant in the header surfaces the invariant up top, which is what WRIT-04 requires)

    **Step 6 — validate:** Run `bundle exec jekyll build --strict` (or the project's standard build command). The build must exit zero with no warnings.
  </action>
  <verify>
    <automated>bundle exec jekyll build --strict 2>&1 | tee /tmp/jekyll-build.log && grep -q "done in" /tmp/jekyll-build.log && ! grep -qiE "warning|error" /tmp/jekyll-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cp.TheConstant' _layouts/chokepoint.html` returns >= 2 (one in `{% if %}`, one in `{{ }}`)
    - `grep -c 'cp-invariant-label' _layouts/chokepoint.html` returns >= 1
    - `grep -c 'THE CONSTANT' _layouts/chokepoint.html` returns >= 1
    - `grep -c 'mdash' _layouts/chokepoint.html` returns 0
    - `grep -n $'\u2014' _layouts/chokepoint.html` returns no matches outside of Liquid `{%- comment -%}` blocks (verify by reading any returned lines)
    - `grep -F 'invariant condition the attacker must satisfy, regardless of tool' _layouts/chokepoint.html` returns 1 match (em dash replaced with comma)
    - `grep -F 'IOK Rule: ' _layouts/chokepoint.html` returns >= 2 matches
    - `grep -F 'Sigma Rule: ' _layouts/chokepoint.html` returns >= 4 matches
    - `bundle exec jekyll build --strict` exits 0 with no `warning` or `error` lines
    - Manual: open the rendered `_site/chokepoints/clickfix-techniques/index.html` and confirm `THE CONSTANT` label appears in the page header HTML before the first `<details>` block: `awk '/THE CONSTANT/{found=1} /<details/{if(found) print "PASS"; exit}' _site/chokepoints/clickfix-techniques/index.html` returns `PASS`
  </acceptance_criteria>
  <done>TheConstant renders in the header of every chokepoint detail page; all `&mdash;` entities and the inline prose em dash removed from chokepoint.html; jekyll build --strict passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove em dash from default layout footer tagline</name>
  <files>_layouts/default.html</files>
  <read_first>
    - _layouts/default.html (full file, focus on footer block around line 32)
  </read_first>
  <action>
    Replace the `&mdash;` entity in the footer tagline at line ~32 with a literal pipe + space, or restructure to two short clauses. Do not touch any other line of the file.

    BEFORE:
    ```html
    &mdash; community detection engineering resource
    ```

    AFTER (option A — simplest, preserves tagline structure):
    ```html
    | community detection engineering resource
    ```

    OR AFTER (option B — if context calls for a sentence break, use a period and capitalize):
    ```html
    . Community detection engineering resource.
    ```

    Read the surrounding line to pick whichever flows better with the project name preceding it. Do not introduce any new HTML elements.

    Validate with `bundle exec jekyll build --strict` (must exit zero, no warnings).
  </action>
  <verify>
    <automated>bundle exec jekyll build --strict 2>&1 | tee /tmp/jekyll-build-default.log && grep -q "done in" /tmp/jekyll-build-default.log && ! grep -qiE "warning|error" /tmp/jekyll-build-default.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mdash' _layouts/default.html` returns 0
    - `grep -n $'\u2014' _layouts/default.html` returns 0 matches
    - `grep -F 'community detection engineering resource' _layouts/default.html` returns 1 match (tagline content preserved)
    - `bundle exec jekyll build --strict` exits 0 with no `warning` or `error` lines
    - Rendered footer in `_site/index.html` still contains the phrase "community detection engineering resource"
  </acceptance_criteria>
  <done>Footer tagline em dash removed; tagline text preserved; jekyll build --strict passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML data → Liquid render → static HTML | Single-author template change; no untrusted input. Liquid auto-escapes `{{ }}` output by default for strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Liquid syntax error from header insertion breaks build | mitigate | `jekyll build --strict` after every template edit; revert immediately on warning |
| T-02-05 | Information Disclosure | TheConstant content is intentionally public | accept | All chokepoint content is public-facing knowledge by design |
| T-02-06 | Denial of Service | Malformed Liquid causes site-wide build failure | mitigate | Edits scoped to specific lines; existing `.cp-invariant` class already in stylesheet (no CSS dependency); `{% if %}` guard prevents nil access |
</threat_model>

<verification>
- `grep -c 'mdash' _layouts/chokepoint.html _layouts/default.html` returns 0 for both files
- `grep -c 'cp.TheConstant' _layouts/chokepoint.html` returns >= 2
- `bundle exec jekyll build --strict` exits 0 with no warnings or errors
- Manual page load: every chokepoint detail page (`/chokepoints/<slug>/`) shows the THE CONSTANT block in the header before the Attack Chokepoints section
</verification>

<success_criteria>
- WRIT-04 satisfied: TheConstant is the visual anchor at the top of every chokepoint detail page
- WRIT-01 template scope satisfied: no `&mdash;` entities or prose em dashes remain in `_layouts/chokepoint.html` or `_layouts/default.html`
- jekyll build --strict passes clean
- No CSS changes, no new visual components, no URL or slug changes (Phase 3 boundaries respected)
</success_criteria>

<output>
After completion, create `.planning/phases/02-writing-and-content-consistency/02-02-SUMMARY.md`
</output>
